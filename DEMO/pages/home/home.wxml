<view class="page">
  <view class="brand-header">
    <view class="logo-section">
      <view class="logo">🎯</view>
      <view class="brand-info"></view>
    </view>
    <view class="header-actions">
      <view class="search-bar">
        <text class="search-icon">🔍</text>
        <input
          class="search-input"
          type="text"
          placeholder="搜索武将，回车直达..."
          value="{{searchKeyword}}"
          confirm-type="search"
          bindinput="onSearchInput"
          bindconfirm="onSearchConfirm"
        />
      </view>
    </view>
  </view>

  <view class="banner-section">
    <swiper class="banner-swiper" indicator-dots="{{true}}" autoplay="{{true}}" circular="{{true}}" interval="5000">
      <swiper-item>
        <view class="banner-item">
          <image class="banner-image" src="" mode="aspectFill" />
          <view class="banner-overlay">
            <view class="banner-title">限时活动</view>
            <view class="banner-subtitle">双倍技能经验</view>
          </view>
        </view>
      </swiper-item>
      <swiper-item>
        <view class="banner-item">
          <image class="banner-image" src="" mode="aspectFill" />
          <view class="banner-overlay">
            <view class="banner-title">新手指南</view>
            <view class="banner-subtitle">快速上手技能系统</view>
          </view>
        </view>
      </swiper-item>
    </swiper>
  </view>

  <view class="quick-entries">
    <view class="section-title">快捷入口</view>
    <view class="entries-grid">
      <view
        class="entry-item"
        wx:for="{{displayedEntries}}"
        wx:key="name"
        bindtap="onEntryTap"
        data-action="{{item.action}}"
      >
        <view class="entry-icon">
          <image class="entry-icon-img" src="{{item.icon}}" mode="aspectFit" />
        </view>
        <view class="entry-text">{{item.name}}</view>
      </view>
    </view>

    <button class="all-generals-btn" bindtap="toggleAllGenerals">
      {{showAllGenerals ? '收起武将' : '全部武将'}}
    </button>

    <view class="tool-row">
      <view class="tool-btn" bindtap="goCardTracker">记牌器</view>
      <view class="tool-btn" bindtap="goHpTracker">体力值</view>
    </view>
  </view>

  <view class="bottom-spacer"></view>
</view>